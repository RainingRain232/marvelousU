// Cooldowns, casting, ability resolution
import type { GameState } from "@sim/state/GameState";

export const AbilitySystem = {
  update(_state: GameState, _dt: number): void {
    // TODO: tick cooldowns, trigger cast logic, delegate to abilities/
  },
};
