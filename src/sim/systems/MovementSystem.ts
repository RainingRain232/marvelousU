// Pathfinding, group movement, formation
import type { GameState } from "@sim/state/GameState";

export const MovementSystem = {
  update(_state: GameState, _dt: number): void {
    // TODO: A* pathfinding, group movement, formation
  },
};
