// Auto-battle unit AI, target selection, retreat logic
import type { GameState } from "@sim/state/GameState";

export const AISystem = {
  update(_state: GameState, _dt: number): void {
    // TODO: target selection, retreat, formation AI
  },
};
